{
  "name": "@agent-deck/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-deck/protocol": "workspace:*",
    "@fastify/cors": "^11.2.0",
    "@fastify/helmet": "^13.0.2",
    "@fastify/static": "^9.1.3",
    "fastify": "^5.8.5",
    "socket.io": "^4.8.3",
    "yaml": "^2.9.0"
  },
  "optionalDependencies": {
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "socket.io-client": "^4.8.3",
    "tsx": "^4.22.3",
    "vitest": "^4.1.7"
  }
}
