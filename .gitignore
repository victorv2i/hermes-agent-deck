node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example
coverage/
test-results/
playwright-report/

# Transient Playwright UX-probe scratch files (repo root)
.ux-*.mjs

# Design-generation scratch (screenshots, image intermediates, gen scripts)
.design-gen/

# Local tool working directories (never track)
.claude/worktrees/

# Local working notes and assistant-tool files, kept out of the public repo
/CLAUDE.md
/STATUS.md

# TypeScript incremental build info
*.tsbuildinfo
